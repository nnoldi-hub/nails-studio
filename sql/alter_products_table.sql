ALTER TABLE products
ADD COLUMN slug VARCHAR(255) AFTER id,
ADD COLUMN title VARCHAR(255) AFTER slug,
ADD COLUMN description TEXT AFTER title,
ADD COLUMN content_list TEXT AFTER description,
ADD COLUMN meta VARCHAR(255) AFTER content_list,
ADD COLUMN image VARCHAR(255) AFTER meta,
ADD COLUMN category VARCHAR(100) AFTER image,
ADD COLUMN level VARCHAR(100) AFTER category,
ADD COLUMN brand VARCHAR(100) AFTER level,
ADD COLUMN accent_color VARCHAR(20) AFTER brand,
ADD COLUMN badge_recommended TINYINT(1) DEFAULT 0 AFTER accent_color,
ADD COLUMN tutorial_link VARCHAR(255) AFTER badge_recommended,
ADD COLUMN price DECIMAL(10,2) AFTER tutorial_link,
ADD COLUMN status VARCHAR(50) AFTER price,
ADD COLUMN is_active TINYINT(1) DEFAULT 1 AFTER status,
ADD COLUMN created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
